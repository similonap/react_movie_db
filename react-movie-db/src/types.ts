export interface Movie {
    id: number
    title: string
    posterURL: string
    imdbId: string
}

export interface Genre {
    name: string;
    image: string;
    link: string;
    icon: string;
}